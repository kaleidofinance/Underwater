# Generated. abis/generated.ts is NOT here on purpose: it is committed so a
# container built from a clone can start without Foundry. See scripts/abis.mjs.
node_modules/
.ponder/
ponder-env.d.ts
generated/

# Secrets — this file holds RPC URLs that are usually credentials, so it never
# gets committed. See README.md and web/.env.local.example.
.env.local
.env
