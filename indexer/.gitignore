# Generated, and two of them are committed anyway: abis/generated.ts and
# vendor/curve.ts are what let a host build this directory on its own, with no
# Foundry and no web/ above it. See scripts/abis.mjs and scripts/curve.mjs.
node_modules/
.ponder/
ponder-env.d.ts
generated/

# Secrets — this file holds RPC URLs that are usually credentials, so it never
# gets committed. See README.md and web/.env.local.example.
.env.local
.env
