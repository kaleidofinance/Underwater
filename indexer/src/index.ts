/**
 * Ponder loads every module under `src/`, so these imports are belt to that braces —
 * they make the registration order explicit and give a single place to see what the
 * indexer watches.
 */
import "./launchpad";
import "./pair";
