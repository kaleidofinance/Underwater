.vercel
.env*

# Cloudflare / @opennextjs/cloudflare build output and local state.
.open-next
.wrangler
# Local bindings and secrets for `wrangler dev` — same reason .env* is ignored.
.dev.vars
# Generated by `wrangler types`; regenerate rather than commit.
cloudflare-env.d.ts
